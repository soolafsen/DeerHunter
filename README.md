# DeerHunter

Build:

```powershell
dotnet build DeerHunter.slnx
```

Test:

```powershell
dotnet test DeerHunter.slnx
```

Run the worker with the default minimal config:

```text
dotnet run --project src/DeerHunter
```

Show help:

```text
dotnet run --project src/DeerHunter -- --help
```
